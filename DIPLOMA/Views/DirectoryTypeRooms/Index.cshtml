@model IEnumerable<DIPLOMA.Models.DirectoryTypeRooms>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TypeRoom)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TypeRoom)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DirectoryTypeRoomsID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DirectoryTypeRoomsID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DirectoryTypeRoomsID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
